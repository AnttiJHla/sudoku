import {Cell} from './cell';

// describe('Cell', () => {
//     let cell0 : Cell;
//     let cell1 : Cell;
//     beforeEach(()=> {
//         cell0 = new Cell(0,0,0);
//         cell1 = new Cell(1,0,0);
//     });

//     it('when cell has 0 as init value, it should have 9 possible values', () => {
//         let pvals=[1,2,3,4,5,6,7,8,9];
//         expect(cell0.pvals).toEqual(pvals);
//     });
//     it('when init value is not 0, it should only have one pval', () => {
//         let pvals=[1];
//         expect(cell1.pvals).toEqual(pvals);
//     });
    

// });
